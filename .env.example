# URL do servidor backend (websocket)
# Se estiver rodando localmente, não precisa definir (vai usar localhost detectado automaticamente)
# Para produção (Render, Railway, Heroku), coloque a URL aqui
VITE_SERVER_URL=https://seu-servidor-backend.com
